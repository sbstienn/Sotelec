<div class="row">
    {{#each usuarios}}
  <div class="col-sm-3">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">{{this.AREA}}</h5>
        <p class="card-text">{{this.CORREOUSUARIO}}</p>
        <p class="card-text">{{this.NOMBREUSUARIO}}</p>
        <a href="#" class="btn btn-primary">Go somewhere</a>
      </div>
    </div>
  </div>
  {{/each}}
</div>

